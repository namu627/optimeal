# 작업 지시서 v2: load_ingredient_price.py 개선

현재 스크립트는 정상 작동하지만 매칭률이 낮다(1,390개 재료 중 73개만 매칭,
196개 품목 매칭 실패). 아래 3가지를 개선한다.
**기존 스크립트의 구조·스타일은 유지하고, 아래 부분만 수정/추가**한다.

---

## 개선 1: 재료명 정규화 매칭 (매칭률 향상)

`match_ingredient_id()` 함수에 정규화 단계를 추가한다.
현재는 완전일치(ingredient_name) → 동의어(synonym_name)만 시도하는데,
그 뒤에 **정규화 매칭 단계**를 추가한다.

### 정규화 규칙 (순서대로 시도)
매칭 실패 목록에서 도출된 실제 패턴이다.

1. **괄호 및 내용 제거**: "수박(일반)" → "수박", "(선)갈치" → "갈치"
   - 정규식: 괄호와 그 안의 내용 삭제 `\(.*?\)`
2. **상태·산지 수식어 제거** (단어 단위로 제거):
   국산, 수입, 냉동, 활, 선, 냉, 깐, 햇, 건, 저장, 생
   - 예: "브로콜리 국산" → "브로콜리", "깐마늘 대서" → "마늘 대서"(다음 단계로)
3. **첫 단어(대표명) 매칭**: 위로도 안 되면 공백 기준 첫 단어로 매칭
   - 예: "복숭아 백도" → "복숭아", "포도 샤인머스캣" → "포도"

각 단계마다 ingredient_name과 synonym_name 양쪽에 대해 매칭 시도.

### ⚠️ 신뢰도 등급 (매우 중요)
정규화 매칭에는 **오매칭 위험**이 있다. 특히 첫 단어 매칭은
"오이맛고추 → 오이"(오매칭! 실제로는 고추)처럼 틀릴 수 있다.
따라서 매칭 방식에 따라 confidence 등급을 매긴다:

- `exact`  : 완전일치 (기존 방식) — 안전
- `synonym`: 동의어 테이블 일치 — 안전
- `high`   : 괄호/수식어만 제거 후 정확히 일치 (예: "수박(일반)"→"수박") — 대체로 안전
- `low`    : 첫 단어로만 매칭 (예: "복숭아 백도"→"복숭아") — **검수 필요**

---

## 개선 2: 2단계 분리 (적재 전 검수)

⚠️ **사용자 결정: 적재 전에 매칭 결과를 CSV로 먼저 확인하고 승인 후 적재한다.**
스크립트를 2개의 모드로 분리한다. (argparse로 --mode 인자 받기)

### 모드 A: `preview` (기본값)
1. API 수집 + 정규화 매칭까지만 수행.
2. **DB에 적재하지 않는다.**
3. 매칭 미리보기 CSV를 출력한다:
   `data/external/price_match_preview_{날짜}.csv`
   컬럼: `pum_nm, matched_ingredient_id, matched_ingredient_name, confidence, avg_price_per_g, sample_days`
   - confidence가 `low`인 행이 위로 오도록 정렬(검수 우선순위).
4. 콘솔에 요약: confidence 등급별 건수, 매칭 실패 수.

### 모드 B: `load`
1. preview와 동일하게 수집·매칭.
2. **단, 제외 목록을 반영**한다:
   `data/external/price_match_exclude.csv`(있으면) 에 적힌 pum_nm은 적재에서 제외.
   (사용자가 검수 후 오매칭 품목을 이 파일에 적어둔다)
3. ingredient_price에 upsert 적재 (기존 로직).

사용 흐름:
```
1) python scripts/load_ingredient_price.py --mode preview
2) (사용자가 CSV 검수 → 오매칭은 price_match_exclude.csv에 기록)
3) python scripts/load_ingredient_price.py --mode load
```

---

## 개선 3: 30일 평균 로직 정리 (정확도)

현재 `collect_price_samples()`는 30일 × 모든 행의 price_per_g를
그냥 한 리스트에 쌓아서, 품목별 거래일수가 다르면 평균이 왜곡된다.
(단위 파싱 실패 로그에서 같은 품목이 30번 중복된 것으로 확인됨)

**2단계 평균으로 변경**한다:
1. 먼저 `(pum_nm, price_date)` 단위로 그 날의 대표 price_per_g를 정한다.
   (같은 날 같은 품목이 여러 행이면 그 날 내에서 평균)
2. 그다음 pum_nm별로 **날짜별 대표값들의 평균**을 낸다.
   즉 "하루 대표가격 N개(날짜수)의 평균"이 되도록.
3. notes에 `sample_days`(평균에 쓰인 실제 거래일수)를 기록.

---

## 유지할 것 (변경 금지)
- 등급 '상' 필터, U_NAME 단위 환산, ON CONFLICT upsert 로직
- .env에서 GARAK_ID/PASSWD 읽기, 예외 시 str(e) 미출력
- 단위 파싱 실패 CSV 출력 (개수단위 7종은 현재 정상적으로 걸러지는 것)

## 작성 후
바로 실행하지 말고 수정된 스크립트 전체를 보여줘. 검토 후 preview 모드부터 실행한다.
