# 작업 지시서 — Docker 환경 라이브러리 버전 고정

> **대상**: Claude Code
> **작업자**: 남유찬
> **프로젝트**: OptiMeal
> **목표**: `optimeal_app` 컨테이너의 Python 라이브러리 버전을 전체 고정하여 어떤 환경에서 재빌드해도 동일한 실행 환경이 재현되도록 한다.
> **산출물**: (1) `requirements.txt` (전체 버전 고정), (2) `docs/DOCKER_IMAGE_TAGS.md` (이미지 태그 문서)

---

## 배경 및 원칙

현재 OptiMeal은 `optimeal_db`, `optimeal_app` 두 컨테이너로 구성되며, `optimeal_app`에서 pandas·scikit-learn·PyTorch·psycopg2 등을 사용한다. 버전이 고정되어 있지 않으면 팀원 PC나 발표용 서버에서 재빌드할 때 라이브러리 버전이 달라져 코드가 깨지거나 결과 수치가 미묘하게 달라질 수 있다.

**핵심 원칙: 지금 정상 동작하는 환경의 버전을 그대로 박제한다.** 최신 버전을 새로 선택하지 않는다. 현재 실행 중인 컨테이너가 기준선(baseline)이다.

---

## 작업 순서

### STEP 1. 현재 환경 스냅샷 수집

아래 명령을 실행하고 결과를 모두 기록한다. **이 값들이 고정의 기준이다.**

```bash
# Python 버전 확인
docker exec optimeal_app python --version

# 설치된 모든 패키지의 정확한 버전 덤프
docker exec optimeal_app pip freeze

# 현재 이미지 이름/태그 확인
docker inspect optimeal_app --format '{{.Config.Image}}'

# 베이스 이미지 확인 (Dockerfile의 FROM 라인)
grep -i '^FROM' Dockerfile
```

> `pip freeze` 결과를 파일로 바로 뽑으려면:
> ```bash
> docker exec optimeal_app pip freeze > requirements.lock.txt
> ```

### STEP 2. requirements.txt 생성 (전체 고정 방식)

`pip freeze` 출력 전체를 `requirements.txt`로 저장한다. 의존성까지 모두 `==`로 고정되어 재현성이 가장 높다.

```bash
docker exec optimeal_app pip freeze > requirements.txt
```

작성 규칙:
- 모든 패키지를 `패키지명==버전` 형식으로 고정한다. (`>=`, `~=`, 버전 없는 줄 금지)
- 파일 상단에 주석으로 **기준 Python 버전**과 **스냅샷 날짜**를 남긴다.
- 코드가 직접 import 하는 핵심 패키지(pandas, scikit-learn, torch, psycopg2, numpy 등)는 상단에 별도 블록으로 모아 주석을 달아 두면 이후 관리가 쉽다.

예시 상단:

```text
# OptiMeal optimeal_app requirements (frozen)
# Python: 3.x.x  (docker exec optimeal_app python --version 결과 기입)
# Snapshot date: 2026-XX-XX
# 생성: docker exec optimeal_app pip freeze
#
# --- 직접 사용 핵심 패키지 ---
# pandas==...
# scikit-learn==...
# torch==...
# psycopg2-binary==...
# --- 이하 의존성 자동 포함 ---
```

### STEP 3. Dockerfile 연결 확인

Dockerfile이 `requirements.txt`를 참조해 설치하는지 확인한다. 없거나 다르면 아래 형태로 맞춘다.

```dockerfile
FROM python:3.x.x-slim   # STEP 1에서 확인한 정확한 Python 패치 버전으로 고정 (예: 3.11.9-slim)

WORKDIR /app
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt
COPY . .
```

> **주의**: `FROM python:3` 또는 `python:latest` 처럼 느슨한 태그는 금지. Python 자체가 흔들리면 라이브러리 고정이 반쪽짜리가 된다. 반드시 패치 버전까지(`3.11.9`) 명시한다.

### STEP 4. 깨끗한 재빌드 검증 (가장 중요)

고정이 실제로 재현되는지 검증한다. 캐시 없이 새로 빌드해서 STEP 1의 버전과 일치하는지 대조한다.

```bash
# 캐시 없이 새 태그로 재빌드
docker build --no-cache -t optimeal_app:vX.X .

# 새로 빌드된 이미지의 패키지 버전이 기준과 일치하는지 확인
docker run --rm optimeal_app:vX.X pip freeze > requirements.rebuild.txt
diff requirements.txt requirements.rebuild.txt
```

- `diff` 결과가 **비어 있어야** 성공(완전 재현).
- 차이가 나면 해당 패키지가 소스 배포판(sdist)에서 빌드 시점 의존성을 다르게 끌어온 경우일 수 있다. 차이 나는 줄을 `requirements.txt`에 명시적으로 추가 고정한다.
- 검증 후 임시 파일(`requirements.lock.txt`, `requirements.rebuild.txt`)은 삭제한다.

### STEP 5. 태그 문서 작성

`docs/DOCKER_IMAGE_TAGS.md`를 작성/갱신한다. (별도 템플릿 파일 참조: `DOCKER_IMAGE_TAGS_템플릿.md`)
- 이번에 빌드한 이미지 태그(`optimeal_app:vX.X`), 빌드 날짜, Python 버전, 주요 패키지 버전을 표에 한 줄 추가한다.
- `requirements.txt`가 함께 커밋된 Git 커밋 해시를 기록해 두면 이미지↔소스 추적이 완전해진다.

---

## 커밋 및 정리

```bash
git add requirements.txt Dockerfile docs/DOCKER_IMAGE_TAGS.md
git commit -m "chore: pin library versions and document image tag vX.X"
```

- 브랜치는 평소대로 `develop`.
- 팀원에게 "이제 `docker build` 시 requirements.txt가 버전을 고정한다"는 점과 새 이미지 태그를 공유한다.

---

## 완료 체크리스트

- [ ] STEP 1 스냅샷(Python 버전 + pip freeze) 확보
- [ ] `requirements.txt` 전체 `==` 고정, 상단 주석(Python 버전·날짜) 포함
- [ ] Dockerfile이 requirements.txt 참조 + `FROM` 패치 버전까지 고정
- [ ] `--no-cache` 재빌드 후 `diff` 결과 비어 있음 확인
- [ ] `docs/DOCKER_IMAGE_TAGS.md`에 이번 태그 행 추가
- [ ] 임시 파일 삭제, develop 브랜치 커밋, 팀 공유
